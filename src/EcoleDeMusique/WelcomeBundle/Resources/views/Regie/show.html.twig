<h1>Regie</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Capv</th>
            <td>{{ entity.capv }}</td>
        </tr>
        <tr>
            <th>Enfantadulte</th>
            <td>{{ entity.enfantAdulte }}</td>
        </tr>
        <tr>
            <th>Cycleparcours</th>
            <td>{{ entity.cycleParcours }}</td>
        </tr>
        <tr>
            <th>Intru2</th>
            <td>{{ entity.intru2 }}</td>
        </tr>
        <tr>
            <th>Intru3</th>
            <td>{{ entity.intru3 }}</td>
        </tr>
        <tr>
            <th>Sommesansremise</th>
            <td>{{ entity.sommeSansRemise }}</td>
        </tr>
        <tr>
            <th>Remise</th>
            <td>{{ entity.remise }}</td>
        </tr>
        <tr>
            <th>Sommeavecremise</th>
            <td>{{ entity.sommeAvecRemise }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('regie') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('regie_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('regie_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
